using LofiEngine.Games;
using Microsoft.Xna.Framework.Graphics;

namespace LofiPlayer
{
	public class PlayerManager : GameManager
	{
        public PlayerManager(GraphicsDevice device)
            : base(device)
        {
        }

		public void Run()
		{
			// this.Pause(false);
		}
	}
}