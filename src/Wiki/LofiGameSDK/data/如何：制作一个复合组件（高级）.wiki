++ 如何：制作一个复合组件（高级）

在设计制作巨型单位时可以遵循下面流程
1. 绘制草图图案，然后导入到空白的项目当中
2. 添加组件并建立连接
3. 生成原型组件类代码，代码中包含了基本的创建模式、优化路径访问。
    原型组件的基本创建模式能够保证每个组件部件都得到创建，并将导出时的状态设置为初始状态。
    组件类的优化访问路径（设置为可断裂的路径使用遍历访问，设置为不可断裂的路径使用引用访问，不确定可选择遍历访问）
4. 将导出代码放入你的游戏库项目中，完成创建模式、控制逻辑等方面的定制。
5. 将游戏库（重新）导入编辑器，使用创建模式创建实例。
    



